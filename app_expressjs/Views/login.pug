doctype html
html
  head
    title Login - Sistema de Clientes
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
  body.bg-light
    .container.mt-5
      .row.justify-content-center
        .col-md-6
          .card.shadow
            .card-body
              h2.text-center.mb-4 Login
              form(action="/usuarios/login", method="POST")
                .mb-3
                  label.form-label(for="email") Email
                  input.form-control(type="email", name="email", required)
                .mb-3
                  label.form-label(for="senha") Senha
                  input.form-control(type="password", name="senha", required)
                button.btn.btn-success.w-100(type="submit") Entrar
              .text-center.mt-3
                a(href="/usuarios/register") Ainda não tem conta? Cadastre-se
